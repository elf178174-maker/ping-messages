<?xml version="1.0" encoding="utf-8"?>
<!--
  Ping deliberately opts out of Android's automatic cloud backup: the message database and the
  device's private encryption keys must never leave the device via a channel the user did not
  explicitly choose. Chat backups are handled by the in-app backup system instead
  (Settings > Chats > Backup), which produces an encrypted archive the user controls.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
