<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext is refused everywhere in release builds; all API and media traffic is HTTPS. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Loopback only, so a self-hosted backend can be exercised from an emulator during
         development. 10.0.2.2 is the host machine as seen from the Android emulator. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
